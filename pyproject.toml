[project]
name = "python-rq-jobrunner"
version = "0.1.0"
description = "FastAPI + RQ job runner (API + worker) with Postgres/Redis."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = []
